---
label: Laureate
hide_body: false
fields:
- name: title_of_the_work
  type: text
  config:
    required: false
  label: Title of the work
- name: type_of_work
  type: select
  default: Article
  config:
    required: true
    options:
    - Article
    - Book
    - Drawing
    - Photo
    - Video
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Type of work
- name: laureate_firstname
  type: text
  config:
    required: true
    min: 1
  label: Laureate firstname
- name: laureate_lastname
  type: text
  config:
    required: true
    min: 1
  label: Laureate lastname
- name: attached_files
  type: image_gallery
  config: {}
  label: Attached files
  description: Only for attachments. Pictures that should be displayed should be added
    to the "picture(s)" field instead.
- name: picture_s_
  type: image_gallery
  config: {}
  label: Picture(s)
